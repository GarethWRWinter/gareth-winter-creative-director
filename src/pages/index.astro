---
import Analytics from '@vercel/analytics/astro';
const meta = {
  title: "Gareth Winter — Creative Director",
  description: "Creative Director with twenty years building brands inside high-performance environments. Sky, Veloforte, Go City, Mindvalley.",
  url: "https://garethwintercreativedirector.com",
};

const experience = [
  {
    brand: "Mindvalley",
    logos: [{ src: "/logos/mindvalley.svg", alt: "Mindvalley", height: "2rem" }],
    role: "Creative Director",
    period: "2024 – Present",
    stat: null,
    description:
      "The world's leading personal transformation platform. Millions of students across one hundred and ninety countries, investing in their health, mindset, and peak performance. I lead creative strategy, own the brand identity, develop the team, and build the AI-powered systems that make exceptional output at scale sustainable. The audience holds itself to a genuine standard of human performance. They notice when the work is exceptional and they share it when it is. The best chapter is still being written.",
  },
  {
    brand: "Go City",
    logos: [{ src: "/logos/go-city.svg", alt: "Go City", height: "2.75rem" }],
    role: "Creative Director",
    period: "2024",
    stat: "30% growth in month one",
    description:
      "A global private equity travel and attractions business operating across thirty-plus cities. The same high-performance thinking applied to a new context: take ownership of the creative operation, diagnose what is holding it back, remove the friction, raise the standard. Thirty percent growth in month one. When the creative output is sharp and the operation is running cleanly, the commercial results follow. Every time.",
  },
  {
    brand: "Veloforte",
    logos: [{ src: "/logos/veloforte.svg", alt: "Veloforte", height: "3.75rem" }],
    role: "Creative Director",
    period: "2022 – 2023",
    stat: "93% revenue growth in 18 months",
    description:
      "The UK's fastest growing natural sports nutrition company. I built the creative team from nothing and established every dimension of the brand: visual identity, tone of voice, packaging design, film production, B2B partnerships, ambassador and influencer infrastructure. I directed every production from brief to delivery. E-commerce grew 60% year-on-year in 2022. Total revenue grew 93% across eighteen months. We made Veloforte the undisputed UK leader in natural sports nutrition. The craft was exceptional. The numbers were the proof.",
  },
  {
    brand: "Le Blanq",
    logos: [{ src: "/logos/leblanq.svg", alt: "Le Blanq", height: "2rem" }],
    logosMarginBottom: "0.6rem",
    role: "Creative Director",
    period: "2021 – 2022",
    stat: null,
    description:
      "Le Blanq is an ultra-premium cycling lifestyle brand combining luxury travel, exceptional hospitality, and the world's most iconic routes. The intersection of athletic performance and high-end experience. The creative challenge was not scale but precision: making something already beautiful feel that way on the page. I worked part-time on creative strategy and brand content alongside Veloforte. The standard the brand demanded made the work sharper.",
  },
  {
    brand: "Sky",
    logosGap: "0.5rem",
    logos: [
      { src: "/logos/sky.svg", alt: "Sky", height: "2rem" },
      { src: "/logos/sky-sports.svg", alt: "Sky Sports", height: "3rem" },
      { src: "/logos/sky-news.svg", alt: "Sky News", height: "3rem" },
    ],
    role: "Senior Creative",
    period: "2010 – 2021",
    stat: "Promax UK Creative of the Year",
    description:
      "Eleven years at one of the world's great media companies. I started at Sky News and built my career across Sky Sports, Entertainment, Movies, and Corporate Communications. I worked alongside journalists on the front line of global news events, Oscar-nominated directors, and the biggest production studios on earth. I contributed to Sky Ocean Rescue, helping shift public behaviour on single-use plastics. And in cycling, the work I led helped inspire 1.7 million people in the UK to get on a bike for the first time. Tour de France victories. Olympic gold medals. Moments that became part of British cultural history. Cycling was one chapter. At year eleven, I was ready for the next one.",
  },
];

const skills = [
  "Creative Direction",
  "AI-Powered Creative Systems",
  "Art Direction & Design",
  "Human-First AI for Brand",
  "Brand Strategy",
  "Copywriting & Scriptwriting",
  "Filmmaking & Photography",
  "Directing & Shoot Production",
  "Packaging & Product Design",
  "Digital & OOH Advertising",
  "Social Media & Influencer",
  "B2B & Retail Partnerships",
  "Team Leadership & Culture",
  "Marketing Strategy & Insight",
];

const work = [
  {
    brand: "Mindvalley",
    title: "Mindvalley at Apple Stores",
    id: "ik7oRkec40w",
  },
  {
    brand: "Sky Creative",
    title: "Promax UK Agency of the Year",
    id: "krkfnPCqWOA",
  },
  {
    brand: "Sky",
    title: "Sky Loves Cycling",
    id: "HZXUrW6yYcQ",
  },
  {
    brand: "Team Sky",
    title: "Ride On",
    id: "gDqoS9YOrJ8",
  },
  {
    brand: "Mindvalley",
    title: "Audiowaves",
    id: "1PQpZAMxL74",
  },
  {
    brand: "Mindvalley",
    title: "States",
    id: "pyJg-QRApKw",
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />

    <!-- Primary -->
    <title>{meta.title}</title>
    <meta name="description" content={meta.description} />
    <meta name="author" content="Gareth Winter" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href={meta.url} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Gareth Winter" />
    <meta property="og:locale" content="en_GB" />
    <meta property="og:title" content={meta.title} />
    <meta property="og:description" content={meta.description} />
    <meta property="og:url" content={meta.url} />
    <meta property="og:image" content={`${meta.url}/gareth-vishen-stage.jpg`} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Gareth Winter — Creative Director" />

    <!-- Twitter / X Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={meta.title} />
    <meta name="twitter:description" content={meta.description} />
    <meta name="twitter:image" content={`${meta.url}/gareth-vishen-stage.jpg`} />

    <!-- JSON-LD: Person schema -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Person",
      "name": "Gareth Winter",
      "jobTitle": "Creative Director",
      "description": "Creative Director with twenty years building brands inside high-performance environments. Sky, Veloforte, Go City, Mindvalley.",
      "url": "https://garethwintercreativedirector.com",
      "image": "https://garethwintercreativedirector.com/gareth-vishen-stage.jpg",
      "sameAs": [
        "https://www.linkedin.com/in/gareth-winter-20035221/",
        "https://instagram.com/garethwinter"
      ],
      "worksFor": {
        "@type": "Organization",
        "name": "Mindvalley",
        "url": "https://www.mindvalley.com"
      },
      "knowsAbout": [
        "Creative Direction",
        "Brand Strategy",
        "AI-Powered Creative Systems",
        "Art Direction",
        "Filmmaking",
        "Photography",
        "Copywriting",
        "Directing & Shoot Production",
        "Packaging & Product Design",
        "Digital Advertising",
        "Social Media",
        "Team Leadership"
      ],
      "alumniOf": [
        { "@type": "Organization", "name": "Sky" },
        { "@type": "Organization", "name": "Veloforte" },
        { "@type": "Organization", "name": "Go City" },
        { "@type": "Organization", "name": "Le Blanq" }
      ]
    })} />

    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,300;12..96,400;12..96,500;12..96,600&family=Schibsted+Grotesk:wght@400;500;700&family=Caveat:wght@500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>

    <!-- ─── HERO ─────────────────────────────────────────────────── -->
    <section class="hero">
      <div class="hero__inner">
        <p class="hero__eyebrow">Creative Director</p>
        <h1 class="hero__name">Gareth<br />Winter</h1>
        <svg class="hero__contour" viewBox="0 0 1000 22" preserveAspectRatio="none" fill="none" aria-hidden="true">
          <path d="M0,15 C60,15 90,7 150,9 C210,11 240,18 300,16 C360,14 390,4 450,7 C510,10 540,17 600,14 C660,11 690,5 750,8 C810,11 840,15 900,12 C950,10 975,13 1000,12"
                stroke="currentColor" stroke-width="1.2" vector-effect="non-scaling-stroke"/>
        </svg>
        <p class="hero__tagline">
          I make things for audiences I belong to.
        </p>
        <a href="#contact" class="hero__cta">Get in touch</a>
      </div>
    </section>

    <!-- ─── INTRO ─────────────────────────────────────────────────── -->
    <section class="intro">
      <div class="container">
        <p class="section-eyebrow">About</p>
        <h2 class="intro__headline">Sport built the discipline.<br />Creativity built the brands.</h2>
        <div class="intro__grid">
          <div class="intro__statement">
            <p>
              In 2012, Sir Bradley Wiggins crossed the finish line on the
              Champs-Élysées, becoming the first British cyclist to win the
              Tour de France, and then went on to win gold at the London
              Olympic Games. I was at Sky. I was part of the team that
              communicated that victory to the nation. Within ten years, 1.7
              million people in the UK had taken up cycling for the first
              time. That is the power of storytelling, and what it can do
              when it earns its place in the world.
            </p>
            <p>
              That era changed me. Sir David Brailsford built British Cycling
              and Team Sky on the philosophy of marginal gains: improve every
              element by one percent and the compound effect is extraordinary.
              I was not just working inside that culture. I was a student of
              it. That thinking became the operating system behind everything
              I have built since. James Clear later used Sir David's marginal
              gains philosophy as the central case study in Atomic Habits,
              one of the most-read performance books of the past decade. When
              I was inside that culture, it was not a concept. It was the way
              we worked every single day. Twenty years, every scale, inside
              genuinely high-performance organisations. The discipline does
              not change. The results follow.
            </p>
            <p>
              I am not just a director of people. I shoot. I write. I direct.
              I edit. I design. I produce. At Mindvalley, we operate at the
              highest level. My time at Sky was foundation-building. I am
              making creative work for an audience that is genuinely trying
              to change their lives. Three million students across one hundred
              and ninety countries. People who flew to Amsterdam from every
              corner of the world to join our live event and stage talks. That
              audience has no tolerance for work that is not honest. And they
              will share everything when it is. Authenticity in creative work
              is not a value. It is a method. The platform I work for now
              demands it every single day.
            </p>
          </div>
          <div class="intro__credentials">
            <div class="credential">
              <span class="credential__number">20+</span>
              <span class="credential__label">Years in the industry</span>
            </div>
            <div class="credential credential--highlight">
              <span class="credential__number">93%</span>
              <span class="credential__label">Revenue growth at Veloforte</span>
            </div>
            <div class="credential">
              <span class="credential__number">1.7M</span>
              <span class="credential__label">People inspired to cycle at Sky</span>
            </div>
            <div class="credential credential--feature">
              <span class="credential__number">3M+</span>
              <span class="credential__label">Students at Mindvalley</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ─── STAGE PHOTO ──────────────────────────────────────────── -->
    <section class="stage-photo">
      <figure class="stage-photo__portrait">
        <img
          src="/gareth-vishen-stage.jpg"
          alt="Gareth Winter on stage at Mindvalley U Event Amsterdam 2025"
          loading="lazy"
        />
      </figure>
      <figure class="stage-photo__wide">
        <img
          src="/mvu-amsterdam-stage.jpg"
          alt="Mindvalley U Event Amsterdam 2025 — Gareth Winter presenting on the main stage"
          loading="lazy"
        />
        <figcaption>Mindvalley U Event, Amsterdam 2025</figcaption>
      </figure>
    </section>

    <!-- ─── PHILOSOPHY ───────────────────────────────────────────── -->
    <section class="philosophy">
      <div class="container">
        <blockquote class="philosophy__quote">
          I have never worked on a brand I did not believe in.
          That is not a coincidence. It is a standard.
        </blockquote>
      </div>
    </section>

    <!-- ─── EXPERIENCE ─────────────────────────────────────────────── -->
    <section class="experience">
      <div class="container">
        <p class="section-eyebrow">Career</p>
        <h2 class="section-title">Experience</h2>
        <div class="experience__list">
          {
            experience.map((role) => (
              <article class="role">
                <div class="role__header">
                  <div class="role__meta">
                    <div class="role__logos" style={[role.logosGap ? `gap:${role.logosGap}` : '', role.logosMarginBottom ? `margin-bottom:${role.logosMarginBottom}` : ''].filter(Boolean).join(';') || undefined}>
                      {role.logos.map((logo) => (
                        <img class="role__logo" src={logo.src} alt={logo.alt} style={logo.height ? `height:${logo.height}` : undefined} />
                      ))}
                    </div>
                    <span class="role__period">{role.period}</span>
                  </div>
                  <p class="role__title">{role.role}</p>
                  {role.stat && (
                    <p class="role__stat">{role.stat}</p>
                  )}
                </div>
                <p class="role__description">{role.description}</p>
              </article>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ─── ROUTE CONTOUR DIVIDER ────────────────────────────────── -->
    <div class="contour-divider" aria-hidden="true">
      <svg viewBox="0 0 1000 22" preserveAspectRatio="none" fill="none">
        <path d="M0,15 C60,15 90,7 150,9 C210,11 240,18 300,16 C360,14 390,4 450,7 C510,10 540,17 600,14 C660,11 690,5 750,8 C810,11 840,15 900,12 C950,10 975,13 1000,12"
              stroke="currentColor" stroke-width="1.2" vector-effect="non-scaling-stroke"/>
        <circle cx="450" cy="7" r="3" fill="#BB6647"/>
      </svg>
    </div>

    <!-- ─── SKY ON-SET PHOTOS ────────────────────────────────────── -->
    <section class="onset-photos">
      <div class="container">
        <p class="onset-photos__label">On Set</p>
      </div>
      <div class="onset-carousel">
        <div class="onset-carousel__track" id="onsetTrack">
          <figure>
            <img src="/sky-production/sky-onset-1.jpg" alt="Gareth Winter directing on set for Sky cycling production" loading="lazy" />
          </figure>
          <figure>
            <img src="/sky-production/sky-onset-2.jpg" alt="Sky cycling shoot production" loading="lazy" />
          </figure>
          <figure>
            <img src="/mvu-onset-1.jpg" alt="Gareth Winter on stage — Mindvalley U Amsterdam 2025" loading="lazy" />
          </figure>
          <figure>
            <img src="/mvu-onset-2.jpg" alt="Gareth Winter presenting — Mindvalley U Amsterdam 2025" loading="lazy" />
          </figure>
          <figure>
            <img src="/sky-production/sky-onset-3.jpg" alt="Team Sky on-set production" loading="lazy" />
          </figure>
          <figure>
            <img src="/sky-production/sky-onset-4.jpg" alt="Sky on-set direction — cycling" loading="lazy" />
          </figure>
          <figure>
            <img src="/DSCF8534.jpg" alt="Gareth Winter on set — cycling production" loading="lazy" style="object-position: 30% center;" />
          </figure>
        </div>
        <div class="onset-carousel__dots" id="onsetDots">
          <button class="onset-carousel__dot active" data-index="0" aria-label="Photo 1"></button>
          <button class="onset-carousel__dot" data-index="1" aria-label="Photo 2"></button>
          <button class="onset-carousel__dot" data-index="2" aria-label="Photo 3"></button>
          <button class="onset-carousel__dot" data-index="3" aria-label="Photo 4"></button>
          <button class="onset-carousel__dot" data-index="4" aria-label="Photo 5"></button>
          <button class="onset-carousel__dot" data-index="5" aria-label="Photo 6"></button>
          <button class="onset-carousel__dot" data-index="6" aria-label="Photo 7"></button>
        </div>
      </div>
    </section>

    <!-- ─── SELECTED WORK ──────────────────────────────────────────── -->
    <section class="work">
      <div class="container">
        <p class="section-eyebrow">Portfolio</p>
        <h2 class="section-title">Selected Work</h2>
        <div class="work__grid">
          {work.slice(0, 2).map((item) => (
            <div
              class="work__item"
              data-video-id={item.id}
              role="button"
              tabindex="0"
              aria-label={`Play ${item.title}`}
            >
              <div
                class="work__thumb"
                style={`background-image: url(https://img.youtube.com/vi/${item.id}/maxresdefault.jpg)`}
              >
                <div class="work__play">
                  <svg viewBox="0 0 24 24" fill="currentColor" width="28" height="28" aria-hidden="true">
                    <path d="M8 5v14l11-7z"/>
                  </svg>
                </div>
              </div>
              <div class="work__meta">
                <p class="work__brand">{item.brand}</p>
                <p class="work__title">{item.title}</p>
              </div>
            </div>
          ))}

          <!-- ─── REELS ROW ─────────────────────────────────────────── -->
          <div class="work__reels-row">
            <div class="reel-wrap">
              <div class="reel-player">
                <video src="/reels/jay-shetty.mp4" poster="/reels/jay-shetty-thumb.jpg" playsinline preload="none"></video>
                <button class="reel-play" aria-label="Play video">
                  <svg width="52" height="52" viewBox="0 0 52 52" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <circle cx="26" cy="26" r="26" fill="rgba(0,0,0,0.45)"/>
                    <polygon points="21,16 38,26 21,36" fill="white"/>
                  </svg>
                </button>
              </div>
              <div class="work__meta">
                <p class="work__brand">Mindvalley</p>
                <p class="work__title">Jay Shetty</p>
              </div>
            </div>
            <div class="reel-wrap">
              <div class="reel-player">
                <video src="/reels/gwyneth-paltrow.mp4" poster="/reels/gwyneth-paltrow-thumb.jpg" playsinline preload="none"></video>
                <button class="reel-play" aria-label="Play video">
                  <svg width="52" height="52" viewBox="0 0 52 52" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <circle cx="26" cy="26" r="26" fill="rgba(0,0,0,0.45)"/>
                    <polygon points="21,16 38,26 21,36" fill="white"/>
                  </svg>
                </button>
              </div>
              <div class="work__meta">
                <p class="work__brand">Mindvalley</p>
                <p class="work__title">Gwyneth Paltrow</p>
              </div>
            </div>
            <div class="reel-wrap">
              <div class="reel-player">
                <video src="/reels/steven-bartlett.mp4" poster="/reels/steven-bartlett-thumb.jpg" playsinline preload="none"></video>
                <button class="reel-play" aria-label="Play video">
                  <svg width="52" height="52" viewBox="0 0 52 52" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <circle cx="26" cy="26" r="26" fill="rgba(0,0,0,0.45)"/>
                    <polygon points="21,16 38,26 21,36" fill="white"/>
                  </svg>
                </button>
              </div>
              <div class="work__meta">
                <p class="work__brand">Mindvalley</p>
                <p class="work__title">Steven Bartlett</p>
              </div>
            </div>
          </div>

          {work.slice(2).map((item) => (
            <div
              class="work__item"
              data-video-id={item.id}
              role="button"
              tabindex="0"
              aria-label={`Play ${item.title}`}
            >
              <div
                class="work__thumb"
                style={`background-image: url(https://img.youtube.com/vi/${item.id}/maxresdefault.jpg)`}
              >
                <div class="work__play">
                  <svg viewBox="0 0 24 24" fill="currentColor" width="28" height="28" aria-hidden="true">
                    <path d="M8 5v14l11-7z"/>
                  </svg>
                </div>
              </div>
              <div class="work__meta">
                <p class="work__brand">{item.brand}</p>
                <p class="work__title">{item.title}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- ─── SKILLS ─────────────────────────────────────────────────── -->
    <section class="skills">
      <div class="container">
        <p class="section-eyebrow">Capabilities</p>
        <h2 class="section-title">What I Do</h2>
        <ul class="skills__grid">
          {skills.map((skill) => <li class="skill">{skill}</li>)}
        </ul>
      </div>
    </section>

    <!-- ─── CONTACT ─────────────────────────────────────────────────── -->
    <section class="contact" id="contact">
      <div class="container">
        <p class="section-eyebrow contact__eyebrow">Get in touch</p>
        <h2 class="contact__heading">Let's talk.</h2>
        <p class="contact__sub">
          Not any job. The right one. If you are building a brand worth
          believing in, and you want someone who will care as much about
          the craft as you do about the outcome, I want to hear from you.
        </p>
        <a href="mailto:garethwrwinter@outlook.com" class="contact__email">
          garethwrwinter@outlook.com
        </a>
        <div class="contact__links">
          <a
            href="https://www.linkedin.com/in/gareth-winter-20035221/"
            target="_blank"
            rel="noopener noreferrer"
            aria-label="LinkedIn"
            class="contact__icon-link"
          >
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
            </svg>
            <span>LinkedIn</span>
          </a>
          <a
            href="https://instagram.com/garethwinter"
            target="_blank"
            rel="noopener noreferrer"
            aria-label="Instagram"
            class="contact__icon-link"
          >
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
            </svg>
            <span>Instagram</span>
          </a>
        </div>
        <div class="contact__signature">
          <span class="contact__signature-text">Gareth</span>
        </div>
      </div>
    </section>

    <script>
      // Fix thumbnails: maxresdefault returns a 120x90 placeholder if unavailable - fall back to hqdefault
      document.querySelectorAll('.work__thumb').forEach(function(thumb) {
        var match = (thumb.style.backgroundImage || '').match(/vi\/([^/]+)\/maxresdefault/);
        if (!match) return;
        var id = match[1];
        var probe = new Image();
        probe.onload = function() {
          if (this.naturalWidth <= 120) {
            thumb.style.backgroundImage = 'url(https://img.youtube.com/vi/' + id + '/hqdefault.jpg)';
          }
        };
        probe.src = 'https://img.youtube.com/vi/' + id + '/maxresdefault.jpg';
      });

      // On-set carousel
      (function() {
        var track  = document.getElementById('onsetTrack');
        var dots   = document.querySelectorAll('#onsetDots .onset-carousel__dot');
        if (!track) return;

        var total     = track.children.length;
        var current   = 0;
        var timer;

        function slideWidth() {
          return track.firstElementChild.offsetWidth + 3;
        }

        function goTo(index) {
          current = ((index % total) + total) % total;
          track.scrollTo({ left: current * slideWidth(), behavior: 'smooth' });
          dots.forEach(function(d, i) {
            d.classList.toggle('active', i === current);
          });
        }

        function advance() { goTo(current + 1); }

        function startTimer() { timer = setInterval(advance, 3000); }
        function stopTimer()  { clearInterval(timer); }

        dots.forEach(function(dot) {
          dot.addEventListener('click', function() {
            stopTimer();
            goTo(parseInt(dot.dataset.index));
            startTimer();
          });
        });

        track.addEventListener('mouseenter', stopTimer);
        track.addEventListener('mouseleave', startTimer);

        track.addEventListener('scroll', function() {
          var idx = Math.round(track.scrollLeft / slideWidth());
          dots.forEach(function(d, i) {
            d.classList.toggle('active', i === idx);
          });
          current = idx;
        }, { passive: true });

        startTimer();
      })();

      // Reel click-to-play
      document.querySelectorAll('.reel-player').forEach(function(player) {
        var video = player.querySelector('video');
        var playBtn = player.querySelector('.reel-play');

        player.addEventListener('click', function() {
          if (video.paused) {
            video.play();
            playBtn.classList.add('hidden');
          } else {
            video.pause();
            playBtn.classList.remove('hidden');
          }
        });

        video.addEventListener('ended', function() {
          playBtn.classList.remove('hidden');
        });
      });

      // Inline YouTube playback
      document.querySelectorAll('.work__item[data-video-id]').forEach(function(item) {
        function playVideo() {
          var id = item.dataset.videoId;
          var thumb = item.querySelector('.work__thumb');
          thumb.style.backgroundImage = 'none';
          thumb.innerHTML = '<iframe src="https://www.youtube.com/embed/' + id + '?autoplay=1" style="position:absolute;inset:0;width:100%;height:100%;border:0;" allow="autoplay; encrypted-media; picture-in-picture" allowfullscreen></iframe>';
        }
        item.addEventListener('click', playVideo);
        item.addEventListener('keydown', function(e) {
          if (e.key === 'Enter' || e.key === ' ') {
            e.preventDefault();
            playVideo();
          }
        });
      });
    </script>

    <Analytics />
  </body>
</html>

<style>
  /* ── ALMANAC Design System ─────────────────────────────────── */
  :root {
    /* Surfaces & ink */
    --paper: #F3F1EA;
    --paper-sunken: #ECE8DE;
    --surface: #FFFFFF;
    --surface-raised: #FBFAF6;
    --ink: #23211C;
    --ink-2: #615B50;
    --ink-3: #948D80;
    --line: #E7E2D7;
    --line-strong: #D6CFC1;
    /* Accents */
    --forest: #36513F;
    --forest-soft: #5C7A63;
    --sage: #C3CDBC;
    --sage-tint: #E9ECE2;
    --clay: #BB6647;
    --clay-soft: #D2855B;
    --dusty: #7C95A3;
    --ochre: #C7A458;
    /* Type */
    --font-display: 'Bricolage Grotesque', 'Segoe UI', sans-serif;
    --font-sans: 'Schibsted Grotesk', -apple-system, 'Segoe UI', Helvetica, sans-serif;
    --font-script: 'Caveat', cursive;
    /* Shape */
    --radius-sm: 4px;
    --radius-md: 6px;
    --radius-full: 9999px;
    --shadow-overlay: 0 2px 8px rgba(33, 30, 26, 0.10);
    /* Layout */
    --max-w: 960px;
    --gap: clamp(1rem, 4vw, 2rem);
  }

  /* ── Reset & Base ──────────────────────────────────────────── */
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html {
    font-size: 16px;
    scroll-behavior: smooth;
  }

  body {
    font-family: var(--font-sans);
    background: var(--paper);
    color: var(--ink);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
  }

  /* ── Layout ────────────────────────────────────────────────── */
  .container {
    max-width: var(--max-w);
    margin: 0 auto;
    padding: 0 var(--gap);
  }

  /* ── Eyebrow label ─────────────────────────────────────────── */
  .section-eyebrow {
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ink-3);
    margin-bottom: 0.75rem;
  }

  /* ── Section Title ─────────────────────────────────────────── */
  .section-title {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 300;
    letter-spacing: -0.01em;
    margin-bottom: 3rem;
    color: var(--ink);
  }

  /* ── Route Contour Divider ─────────────────────────────────── */
  .contour-divider {
    background: var(--paper-sunken);
    color: var(--line-strong);
    line-height: 0;
    padding: 0;
  }

  .contour-divider svg {
    width: 100%;
    height: 22px;
    display: block;
  }

  /* ── Hero ──────────────────────────────────────────────────── */
  .hero {
    background: var(--paper);
    min-height: 100svh;
    display: flex;
    align-items: flex-end;
    padding: clamp(3rem, 8vw, 6rem) 0;
    border-bottom: 1px solid var(--line);
  }

  .hero__inner {
    max-width: var(--max-w);
    margin: 0 auto;
    padding: 0 var(--gap);
    width: 100%;
  }

  .hero__eyebrow {
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--forest);
    margin-bottom: 1.5rem;
  }

  .hero__name {
    font-family: var(--font-display);
    font-size: clamp(4rem, 14vw, 10rem);
    font-weight: 300;
    line-height: 0.92;
    letter-spacing: -0.03em;
    color: var(--ink);
    margin-bottom: 2rem;
  }

  .hero__contour {
    width: 100%;
    max-width: 480px;
    height: 22px;
    color: var(--line-strong);
    margin-bottom: 1.75rem;
    display: block;
  }

  .hero__tagline {
    font-family: var(--font-display);
    font-style: italic;
    font-size: clamp(1.1rem, 2.5vw, 1.5rem);
    font-weight: 300;
    color: var(--ink-2);
    max-width: 480px;
    line-height: 1.4;
    margin-bottom: 3rem;
  }

  .hero__cta {
    display: inline-block;
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--surface);
    background: var(--forest);
    padding: 0.875rem 2rem;
    border-radius: var(--radius-sm);
    transition: background 0.2s ease;
  }

  .hero__cta:hover {
    background: var(--forest-soft);
  }

  /* ── Intro ─────────────────────────────────────────────────── */
  .intro {
    background: var(--surface);
    padding: clamp(4rem, 10vw, 8rem) 0;
    border-bottom: 1px solid var(--line);
  }

  .intro__headline {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 300;
    line-height: 1.08;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin-bottom: clamp(2.5rem, 5vw, 4rem);
    max-width: 18ch;
  }

  .intro__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
  }

  @media (min-width: 700px) {
    .intro__grid {
      grid-template-columns: 3fr 2fr;
      gap: 6rem;
      align-items: start;
    }
  }

  .intro__statement p {
    font-family: var(--font-sans);
    font-size: clamp(1rem, 1.5vw, 1.0625rem);
    line-height: 1.8;
    color: var(--ink-2);
    margin-bottom: 1.5rem;
  }

  .intro__statement p:last-child {
    margin-bottom: 0;
  }

  .intro__credentials {
    display: flex;
    flex-direction: column;
    gap: 0;
    border: 1px solid var(--line);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .credential {
    padding: 1.5rem;
    border-bottom: 1px solid var(--line);
  }

  .credential:last-child {
    border-bottom: none;
  }

  .credential--highlight .credential__number {
    color: var(--clay);
  }

  .credential--feature {
    background: var(--forest);
  }

  .credential--feature .credential__number {
    color: var(--surface);
  }

  .credential--feature .credential__label {
    color: rgba(255, 255, 255, 0.65);
  }

  .credential__number {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 5vw, 3.25rem);
    font-weight: 300;
    line-height: 1;
    letter-spacing: -0.02em;
    font-variant-numeric: tabular-nums;
    color: var(--forest);
    margin-bottom: 0.4rem;
  }

  .credential__label {
    display: block;
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ink-3);
  }

  /* ── Stage Photo ───────────────────────────────────────────── */
  .stage-photo {
    display: grid;
    grid-template-columns: 1fr;
  }

  @media (min-width: 900px) {
    .stage-photo {
      grid-template-columns: 3fr 5fr;
    }
  }

  .stage-photo figure {
    margin: 0;
    padding: 0;
    overflow: hidden;
    position: relative;
  }

  .stage-photo__portrait {
    background: #1a1a1a;
    aspect-ratio: 4/5;
  }

  @media (min-width: 900px) {
    .stage-photo__portrait {
      aspect-ratio: auto;
      min-height: 560px;
    }
  }

  .stage-photo__wide {
    background: #0d0d0d;
    aspect-ratio: 16/9;
  }

  @media (min-width: 900px) {
    .stage-photo__wide {
      aspect-ratio: auto;
    }
  }

  .stage-photo figure img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
    display: block;
  }

  .stage-photo__portrait img {
    object-position: center 15%;
  }

  .stage-photo figcaption {
    position: absolute;
    bottom: 1.25rem;
    right: 1.5rem;
    font-family: var(--font-sans);
    font-size: 0.65rem;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.4);
  }

  /* ── Philosophy ────────────────────────────────────────────── */
  .philosophy {
    background: var(--ink);
    padding: clamp(3rem, 8vw, 5rem) 0;
  }

  .philosophy__quote {
    font-family: var(--font-display);
    font-style: italic;
    font-size: clamp(1.4rem, 3.5vw, 2.25rem);
    font-weight: 300;
    line-height: 1.35;
    letter-spacing: -0.01em;
    color: var(--surface);
    max-width: 720px;
    border-left: 2px solid var(--forest);
    padding-left: 2rem;
  }

  /* ── Experience ────────────────────────────────────────────── */
  .experience {
    background: var(--paper-sunken);
    padding: clamp(4rem, 10vw, 8rem) 0;
  }

  .experience__list {
    display: flex;
    flex-direction: column;
  }

  .role {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
    padding: 2.5rem 0;
    border-top: 1px solid var(--line-strong);
  }

  .role:last-child {
    border-bottom: 1px solid var(--line-strong);
  }

  @media (min-width: 700px) {
    .role {
      grid-template-columns: 1fr 2fr;
      gap: 4rem;
      align-items: start;
    }
  }

  .role__logos {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: flex-start;
    gap: 1.25rem;
    line-height: 0;
    margin-bottom: 0.75rem;
  }

  .role__logo {
    display: block;
    height: 2rem;
    width: auto;
    object-fit: contain;
    object-position: left center;
  }

  .role__period {
    display: block;
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ink-3);
    margin-bottom: 0.5rem;
  }

  .role__title {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--ink-2);
  }

  .role__stat {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1rem;
    font-weight: 400;
    color: var(--clay);
    margin-top: 0.75rem;
  }

  .role__description {
    font-family: var(--font-sans);
    font-size: 1rem;
    line-height: 1.8;
    color: var(--ink-2);
    margin-top: 0.5rem;
  }

  @media (min-width: 700px) {
    .role__description {
      margin-top: 0;
    }
  }

  /* ── On-Set Carousel ───────────────────────────────────────── */
  .onset-photos {
    background: var(--ink);
    padding-top: clamp(2rem, 5vw, 3.5rem);
    padding-bottom: clamp(2rem, 5vw, 3.5rem);
  }

  .onset-photos__label {
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ink-3);
    margin-bottom: 1.25rem;
  }

  .onset-carousel {
    position: relative;
    margin-top: 1.25rem;
  }

  .onset-carousel__track {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scrollbar-width: none;
    -ms-overflow-style: none;
    gap: 3px;
  }

  .onset-carousel__track::-webkit-scrollbar {
    display: none;
  }

  .onset-carousel__track figure {
    flex: 0 0 calc(25% - 3px);
    margin: 0;
    padding: 0;
    overflow: hidden;
    aspect-ratio: 4/3;
    background: #1a1a1a;
    scroll-snap-align: start;
  }

  @media (max-width: 700px) {
    .onset-carousel__track figure {
      flex: 0 0 calc(80% - 3px);
    }
  }

  .onset-carousel__track figure img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
    transition: transform 0.4s ease;
  }

  .onset-carousel__track figure:hover img {
    transform: scale(1.02);
  }

  /* Dots */
  .onset-carousel__dots {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 1.25rem;
  }

  .onset-carousel__dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    border: none;
    background: rgba(255, 255, 255, 0.2);
    cursor: pointer;
    padding: 0;
    transition: background 0.2s ease, transform 0.2s ease;
  }

  .onset-carousel__dot.active {
    background: var(--forest-soft);
    transform: scale(1.3);
  }

  /* ── Selected Work ─────────────────────────────────────────── */
  .work {
    background: var(--surface);
    padding: clamp(4rem, 10vw, 8rem) 0;
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .work__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  @media (max-width: 600px) {
    .work__grid {
      grid-template-columns: 1fr;
    }
  }

  .work__item {
    color: var(--ink);
    display: block;
    cursor: pointer;
  }

  .work__thumb {
    aspect-ratio: 16/9;
    background-size: cover;
    background-position: center;
    background-color: var(--paper-sunken);
    position: relative;
    overflow: hidden;
    border-radius: var(--radius-md);
    border: 1px solid var(--line);
  }

  .work__play {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(35, 33, 28, 0.3);
    color: var(--surface);
    opacity: 0;
    transition: opacity 0.2s ease;
    border-radius: var(--radius-md);
  }

  .work__play svg {
    filter: drop-shadow(0 2px 8px rgba(0,0,0,0.5));
  }

  .work__item:hover .work__play {
    opacity: 1;
  }

  .work__item:hover .work__thumb {
    transform: scale(1.01);
    transition: transform 0.3s ease;
  }

  .work__meta {
    padding: 0.875rem 0 0;
  }

  .work__brand {
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--forest);
    margin-bottom: 0.25rem;
  }

  .work__title {
    font-family: var(--font-sans);
    font-size: 0.9rem;
    font-weight: 400;
    color: var(--ink-2);
    line-height: 1.4;
  }

  /* ── Reels row (inside work grid) ─────────────────────────── */
  .work__reels-row {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  @media (max-width: 600px) {
    .work__reels-row {
      grid-template-columns: 1fr;
    }
  }

  /* ── Social Reels ──────────────────────────────────────────── */
  :global(.reel-wrap) {
    display: flex;
    flex-direction: column;
  }

  :global(.reel-player) {
    position: relative;
    aspect-ratio: 9/16;
    background: var(--ink);
    overflow: hidden;
    cursor: pointer;
    border-radius: var(--radius-md);
    border: 1px solid var(--line);
  }

  :global(.reel-player video) {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
  }

  :global(.reel-play) {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: none;
    border: none;
    cursor: pointer;
    transition: opacity 0.2s ease;
  }

  :global(.reel-play.hidden) {
    opacity: 0;
    pointer-events: none;
  }

  /* ── Skills ────────────────────────────────────────────────── */
  .skills {
    background: var(--paper);
    padding: clamp(4rem, 10vw, 8rem) 0;
  }

  .skills__grid {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0;
  }

  @media (min-width: 700px) {
    .skills__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .skill {
    font-family: var(--font-sans);
    font-size: 0.9rem;
    font-weight: 400;
    color: var(--ink-2);
    padding: 0.875rem 1rem 0.875rem 0;
    border-top: 1px solid var(--line);
  }

  /* Highlight the two flagship capabilities */
  .skill:nth-child(1),
  .skill:nth-child(2) {
    font-weight: 500;
    color: var(--forest);
  }

  /* ── Contact ───────────────────────────────────────────────── */
  .contact {
    background: var(--ink);
    color: var(--surface);
    padding: clamp(5rem, 12vw, 10rem) 0;
  }

  .contact__eyebrow {
    color: var(--ink-3);
  }

  .contact__heading {
    font-family: var(--font-display);
    font-size: clamp(3rem, 8vw, 6rem);
    font-weight: 300;
    line-height: 0.95;
    letter-spacing: -0.02em;
    color: var(--surface);
    margin-bottom: 1.75rem;
    margin-top: 0.5rem;
  }

  .contact__sub {
    font-family: var(--font-sans);
    font-size: clamp(0.95rem, 1.5vw, 1.05rem);
    color: rgba(251, 250, 246, 0.55);
    margin-bottom: 3rem;
    font-weight: 400;
    max-width: 560px;
    line-height: 1.75;
  }

  .contact__email {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(1.2rem, 3vw, 1.75rem);
    font-weight: 300;
    color: var(--forest-soft);
    text-decoration: none;
    margin-bottom: 2.5rem;
    letter-spacing: -0.01em;
    transition: color 0.2s ease;
  }

  .contact__email:hover {
    color: var(--sage);
  }

  .contact__links {
    display: flex;
    gap: 2rem;
    align-items: center;
    margin-bottom: 3rem;
  }

  .contact__icon-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(251, 250, 246, 0.35);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .contact__icon-link:hover {
    color: var(--surface);
  }

  .contact__icon-link svg {
    flex-shrink: 0;
    opacity: 0.6;
    transition: opacity 0.2s ease;
  }

  .contact__icon-link:hover svg {
    opacity: 1;
  }

  .contact__signature {
    border-top: 1px solid rgba(231, 226, 215, 0.12);
    padding-top: 2rem;
  }

  .contact__signature-text {
    font-family: var(--font-script);
    font-size: 42px;
    font-weight: 500;
    color: var(--forest-soft);
    display: block;
    line-height: 1;
  }
</style>
